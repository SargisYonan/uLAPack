language: c
sudo: false

before_script: 
  - pushd test/
  - make
  - popd

script:
  - pushd bin/
  - ./ulapack_test_static
  - ./ulapack_test_dynamic
  - popd

after_script:
  - pushd test/
  - make clean
  - popd